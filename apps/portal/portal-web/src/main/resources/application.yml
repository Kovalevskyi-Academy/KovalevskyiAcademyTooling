#debug: true

server:
  error:
    whitelabel:
      enabled: false

pebble:
  suffix: .peb
  cache: false
  strictVariables: true

spring:
  application:
    name: "Project Pygmalion"
  profiles:
    active: dev

---
spring.config.activate.on-profile: dev

env: dev

server:
  port: 8080
